{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "실시간 Zone 데이터 업데이트",
  "description": "센서 시뮬레이터에서 프런트엔드 대시보드로 전송되는 Zone 객체 배열입니다.",
  "type": "array",
  "items": {
    "type": "object",
    "description": "하나의 물류 Zone에 대한 집계된 실시간 상태를 나타냅니다.",
    "properties": {
      "id": {
        "description": "Zone의 고유 식별자 (예: 'PK-01').",
        "type": "string"
      },
      "name": {
        "description": "사람이 읽을 수 있는 Zone의 이름 (예: 'Picking Zone').",
        "type": "string"
      },
      "type": {
        "description": "Zone의 기능적 유형입니다.",
        "type": "string",
        "enum": ["entry", "process", "buffer", "exit", "qc", "sorting"]
      },
      "lines": {
        "description": "Zone 내의 컨베이어 라인 수.",
        "type": "integer"
      },
      "length": {
        "description": "컨베이어/구역의 총 길이 (미터 단위).",
        "type": "integer"
      },
      "workers": {
        "description": "현재 Zone에 할당된 작업자 수.",
        "type": "integer"
      },
      "load": {
        "description": "Zone의 평균 가동률/부하량 (백분율, 0-100).",
        "type": "number"
      },
      "temp": {
        "description": "Zone의 평균 온도 (섭씨). 현재는 시뮬레이션되지 않는 예비 필드입니다.",
        "type": "number"
      },
      "vib": {
        "description": "Zone의 평균 진동 수준.",
        "type": "number"
      },
      "status": {
        "description": "Zone의 전반적인 운영 상태.",
        "type": "string",
        "enum": ["normal", "warning", "critical"]
      },
      "timestamp": {
        "description": "데이터가 생성된 시점의 ISO 8601 타임스탬프.",
        "type": "string",
        "format": "date-time"
      }
    },
    "required": [
      "id",
      "name",
      "type",
      "load",
      "temp",
      "vib",
      "status",
      "timestamp"
    ]
  }
}
